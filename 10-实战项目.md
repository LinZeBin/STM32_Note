# 实战项目与完整示例

## 1. 项目1: LED流水灯

### 1.1 硬件连接

```
STM32F407        LED
├─ PD12 ─────────────────────▶ LED1 (绿色)
├─ PD13 ─────────────────────▶ LED2 (橙色)
├─ PD14 ─────────────────────▶ LED3 (红色)
├─ PD15 ─────────────────────▶ LED4 (蓝色)
└─ GND ──────────────────────▶ 公共GND

每个LED通过330Ω电阻接地
```

### 1.2 完整代码

```c
#include "stm32f4xx_hal.h"

void GPIO_LED_Init(void)
{
    __HAL_RCC_GPIOD_CLK_ENABLE();
    
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    GPIO_InitStruct.Pin = GPIO_PIN_12 | GPIO_PIN_13 | GPIO_PIN_14 | GPIO_PIN_15;
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    
    HAL_GPIO_Init(GPIOD, &GPIO_InitStruct);
    
    // 初始状态：所有LED关闭
    HAL_GPIO_WritePin(GPIOD, GPIO_PIN_12 | GPIO_PIN_13 | GPIO_PIN_14 | GPIO_PIN_15, GPIO_PIN_RESET);
}

void LED_Flowing_Light(void)
{
    uint16_t leds[] = {GPIO_PIN_12, GPIO_PIN_13, GPIO_PIN_14, GPIO_PIN_15};
    
    for(int i = 0; i < 4; i++) {
        HAL_GPIO_WritePin(GPIOD, leds[i], GPIO_PIN_SET);     // 点亮
        HAL_Delay(200);
        HAL_GPIO_WritePin(GPIOD, leds[i], GPIO_PIN_RESET);   // 熄灭
    }
}

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    GPIO_LED_Init();
    
    while(1) {
        LED_Flowing_Light();
    }
}
```

---

## 2. 项目2: 温度采集系统

### 2.1 硬件连接

```
STM32F407          温度传感器 (NTC 10K)
├─ PA0 ◀─────────────────────────────── NTC中点
├─ +3.3V ────────────────────────────── 供电
└─ GND ──────────────────────────────── 地

分压电阻: 10K 到 GND
```

### 2.2 温度计算

```c
#include <math.h>

#define R0              10000           // 25℃时的阻值
#define BETA            3977            // β系数
#define T0              (25 + 273.15)   // 25℃ = 298.15K
#define R_SERIES        10000           // 分压电阻

float Get_Temperature(void)
{
    // ADC采样
    uint16_t adc_val = ADC_GetValue();
    
    // ADC转电压
    float voltage = adc_val * 3.3f / 4095;
    
    // 计算NTC阻值
    float R_ntc = R_SERIES * voltage / (3.3f - voltage);
    
    // Steinhart-Hart方程
    float ln_r = logf(R_ntc / R0);
    float T = 1.0f / (1.0f / T0 + ln_r / BETA);
    
    return T - 273.15;  // 转换为摄氏度
}

void Display_Temperature(void)
{
    float temp = Get_Temperature();
    printf("Temperature: %.2f°C\n", temp);
}
```

### 2.3 完整系统

```c
#include "stm32f4xx_hal.h"
#include <stdio.h>
#include <math.h>

ADC_HandleTypeDef hadc1;
UART_HandleTypeDef huart1;

void ADC1_Init(void)
{
    __HAL_RCC_ADC1_CLK_ENABLE();
    __HAL_RCC_GPIOA_CLK_ENABLE();
    
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    GPIO_InitStruct.Pin = GPIO_PIN_0;
    GPIO_InitStruct.Mode = GPIO_MODE_ANALOG;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
    
    hadc1.Instance = ADC1;
    hadc1.Init.ClockPrescaler = ADC_CLOCK_SYNC_PCLK_DIV2;
    hadc1.Init.Resolution = ADC_RESOLUTION_12B;
    hadc1.Init.ScanConvMode = DISABLE;
    hadc1.Init.ContinuousConvMode = DISABLE;
    hadc1.Init.DiscontinuousConvMode = DISABLE;
    hadc1.Init.DataAlign = ADC_DATAALIGN_RIGHT;
    hadc1.Init.NbrOfConversion = 1;
    
    HAL_ADC_Init(&hadc1);
    
    ADC_ChannelConfTypeDef sConfig = {0};
    sConfig.Channel = ADC_CHANNEL_0;
    sConfig.Rank = 1;
    sConfig.SamplingTime = ADC_SAMPLETIME_84CYCLES;
    
    HAL_ADC_ConfigChannel(&hadc1, &sConfig);
}

uint16_t ADC_GetValue(void)
{
    HAL_ADC_Start(&hadc1);
    HAL_ADC_PollForConversion(&hadc1, 100);
    uint16_t value = HAL_ADC_GetValue(&hadc1);
    HAL_ADC_Stop(&hadc1);
    return value;
}

// UART初始化（见前面章节）
// printf重定向（见前面章节）

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    ADC1_Init();
    UART1_Init();
    
    printf("Temperature Monitoring System Started\n");
    
    while(1) {
        float temp = Get_Temperature();
        printf("Temp: %.2f°C\n", temp);
        HAL_Delay(1000);  // 每秒采样一次
    }
}
```

---

## 3. 项目3: PWM呼吸灯

### 3.1 硬件连接

```
STM32F407          LED
├─ PA6 (TIM3_CH1) ──┬───────────────▶ LED阳极
└─ GND ─────────────┴───────────────▶ LED阴极

使用330Ω限流电阻
```

### 3.2 完整代码

```c
#include "stm32f4xx_hal.h"

TIM_HandleTypeDef htim3;

void TIM3_PWM_Init(void)
{
    __HAL_RCC_TIM3_CLK_ENABLE();
    __HAL_RCC_GPIOA_CLK_ENABLE();
    
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    GPIO_InitStruct.Pin = GPIO_PIN_6;
    GPIO_InitStruct.Mode = GPIO_MODE_AF_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    GPIO_InitStruct.Alternate = GPIO_AF2_TIM3;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
    
    // 配置定时器: 20KHz, 占空比0-100%可调
    htim3.Instance = TIM3;
    htim3.Init.Prescaler = 8 - 1;              // 168MHz / 8 = 21MHz
    htim3.Init.CounterMode = TIM_COUNTERMODE_UP;
    htim3.Init.Period = 1050 - 1;              // 21MHz / 1050 = 20KHz
    htim3.Init.ClockDivision = TIM_CLOCKDIVISION_DIV1;
    
    HAL_TIM_PWM_Init(&htim3);
    
    // 配置输出比较
    TIM_OC_InitTypeDef sConfigOC = {0};
    sConfigOC.OCMode = TIM_OCMODE_PWM1;
    sConfigOC.Pulse = 525;                  // 初始占空比 50%
    sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;
    sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;
    
    HAL_TIM_PWM_ConfigChannel(&htim3, &sConfigOC, TIM_CHANNEL_1);
    HAL_TIM_PWM_Start(&htim3, TIM_CHANNEL_1);
}

void PWM_SetDuty(uint32_t duty_percent)
{
    // duty_percent: 0-100
    uint32_t pulse = (htim3.Init.Period + 1) * duty_percent / 100;
    __HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, pulse);
}

void LED_Breathing_Effect(void)
{
    // 渐亮
    for(int i = 0; i <= 100; i++) {
        PWM_SetDuty(i);
        HAL_Delay(10);
    }
    
    // 渐暗
    for(int i = 100; i >= 0; i--) {
        PWM_SetDuty(i);
        HAL_Delay(10);
    }
}

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    TIM3_PWM_Init();
    
    while(1) {
        LED_Breathing_Effect();
    }
}
```

---

## 4. 项目4: 数据采集与显示

### 4.1 系统架构

```
ADC采样 → 数据处理 → 串口输出 → PC显示
   ↓
 DMA缓冲
   ↓
 数据滤波
   ↓
 统计分析
```

### 4.2 完整代码

```c
#include "stm32f4xx_hal.h"
#include <stdio.h>

#define ADC_BUFFER_SIZE  100
#define FILTER_SIZE      10

uint32_t adc_buffer[ADC_BUFFER_SIZE];
uint32_t filter_buffer[FILTER_SIZE];

// 均值滤波
uint32_t Mean_Filter(void)
{
    uint32_t sum = 0;
    for(int i = 0; i < FILTER_SIZE; i++) {
        sum += filter_buffer[i];
    }
    return sum / FILTER_SIZE;
}

// 最大值
uint32_t Get_Max(uint32_t *buf, int len)
{
    uint32_t max = buf[0];
    for(int i = 1; i < len; i++) {
        if(buf[i] > max) max = buf[i];
    }
    return max;
}

// 最小值
uint32_t Get_Min(uint32_t *buf, int len)
{
    uint32_t min = buf[0];
    for(int i = 1; i < len; i++) {
        if(buf[i] < min) min = buf[i];
    }
    return min;
}

void Display_Statistics(void)
{
    uint32_t max = Get_Max(adc_buffer, ADC_BUFFER_SIZE);
    uint32_t min = Get_Min(adc_buffer, ADC_BUFFER_SIZE);
    uint32_t avg = Mean_Filter();
    
    printf("Max: %u, Min: %u, Avg: %u\n", max, min, avg);
}

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    ADC1_Init();
    UART1_Init();
    
    printf("Data Acquisition System Started\n");
    
    while(1) {
        // 采集数据
        for(int i = 0; i < ADC_BUFFER_SIZE; i++) {
            adc_buffer[i] = ADC_GetValue();
            HAL_Delay(10);
        }
        
        // 更新滤波缓冲
        for(int i = 0; i < FILTER_SIZE; i++) {
            filter_buffer[i] = adc_buffer[ADC_BUFFER_SIZE - FILTER_SIZE + i];
        }
        
        // 显示统计
        Display_Statistics();
    }
}
```

---

## 常见问题

### Q1: LED不亮?

✅ 检查GPIO引脚配置
✅ 检查LED极性
✅ 测量GPIO输出电压
✅ 检查限流电阻

### Q2: ADC采样值不稳定?

✅ 增加采样时间
✅ 使用滤波
✅ 检查模拟电源去耦
✅ 远离干扰源

### Q3: PWM频率不对?

✅ 检查时钟配置
✅ 验证PSC和ARR计算
✅ 使用示波器测量
✅ 查看定时器寄存器值

### Q4: 串口无法通信?

✅ 检查波特率设置
✅ 检查GPIO复用功能
✅ 检查TX/RX接线
✅ 使用串口分析仪查看波形

