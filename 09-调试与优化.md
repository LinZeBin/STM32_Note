# 调试与性能优化

## 1. JTAG/SWD调试接口

### 1.1 调试接口对比

```
JTAG (Joint Test Action Group):
├─ 5线接口 (TCK, TMS, TDI, TDO, GND)
├─ 速度: 较慢 (用于测试)
├─ 兼容性: 广泛
└─ 应用: 老旧芯片

SWD (Serial Wire Debug) ⭐ 推荐:
├─ 2线接口 (SWCLK, SWDIO, GND)
├─ 速度: 更快
├─ 占用引脚少
├─ 应用: 现代STM32芯片
└─ 易于集成到PCB

接线 (SWD):
STM32  ← 调试器
├─ SWCLK (PA14) → SWCLK
├─ SWDIO (PA13) → SWDIO
├─ GND → GND
└─ 3.3V → 3.3V (可选供电)
```

### 1.2 启用调试接口

```c
// 默认情况下，JTAG和SWD都启用
// 可通过配置禁用以释放GPIO

void Enable_SWD_Only(void)
{
    // 禁用JTAG，保留SWD
    // 需要在启动文件中配置
    // DBGMCU->CR |= DBGMCU_CR_TRACE_IOEN;
}

void Disable_Debug_Interface(void)
{
    // 禁用调试接口 (释放PA13/PA14)
    // 注意: 禁用后无法通过JTAG/SWD调试！
    __HAL_DBGMCU_DISABLE();
}
```

---

## 2. 调试技巧

### 2.1 Keil MDK调试

```
常用快捷键:
├─ F5: 开始/停止调试
├─ Ctrl+F5: 重新启动
├─ F6: 单步执行 (跳过函数)
├─ F7: 单步执行 (进入函数)
├─ F8: 运行至下一断点
├─ F9: 运行
├─ Ctrl+G: 运行至当前行
└─ Ctrl+Shift+I: 显示变量值

调试窗口:
├─ Watch: 监测变量
├─ Memory: 内存查看
├─ Registers: 寄存器
├─ Disassembly: 反汇编
├─ Peripherals: 外设配置
└─ Logic Analyzer: 逻辑分析
```

### 2.2 打印调试

```c
// printf重定向到UART进行调试输出

int fputc(int ch, FILE *f)
{
    HAL_UART_Transmit(&huart1, (uint8_t *)&ch, 1, 100);
    return ch;
}

// 使用示例
printf("ADC Value: %d\n", adc_value);
printf("Temperature: %.2f°C\n", temperature);
```

### 2.3 内存调试

```c
// 查看当前内存使用情况

void Debug_Memory_Info(void)
{
    extern uint32_t _estack;        // 栈顶
    extern uint32_t _sdata, _edata; // 数据段
    extern uint32_t _sbss, _ebss;   // BSS段
    
    uint32_t stack_top = (uint32_t)&_estack;
    uint32_t heap_start = (uint32_t)&_ebss;
    
    // 栈空间: 栈顶 - 堆顶
    uint32_t stack_space = stack_top - heap_start;
    
    printf("Stack Top: 0x%08X\n", stack_top);
    printf("Heap Start: 0x%08X\n", heap_start);
    printf("Free Space: %u bytes\n", stack_space);
}

// 检测栈溢出
void Check_Stack_Overflow(void)
{
    uint32_t *sp = (uint32_t *)__get_MSP();
    
    // 栈顶应该指向SRAM范围内
    if((uint32_t)sp < 0x20000000 || (uint32_t)sp > 0x20030000) {
        // 栈溢出！
        while(1);
    }
}
```

---

## 3. 性能分析

### 3.1 代码执行时间测量

```c
// 使用SysTick计数执行时间 (毫秒级)
uint32_t Get_Execution_Time_ms(void (*func)(void))
{
    uint32_t start = HAL_GetTick();
    func();
    uint32_t end = HAL_GetTick();
    return end - start;
}

// 使用硬件定时器获得微秒级精度
#include <stdint.h>

uint32_t start_time, end_time;

void Measure_With_Timer(void)
{
    // 初始化定时器在0处
    TIM2->CNT = 0;
    TIM2->CR1 |= TIM_CR1_CEN;
    
    // 记录开始时间
    start_time = TIM2->CNT;
    
    // 执行被测代码
    ADC_GetValue();
    
    // 记录结束时间
    end_time = TIM2->CNT;
    
    // 计算执行时间 (µs)
    uint32_t execution_time = (end_time - start_time) * 1000000 / 168000000;
    
    printf("Execution time: %u µs\n", execution_time);
}

// 使用DWT (Data Watchpoint and Trace) 进行精确计时
void DWT_Init(void)
{
    CoreDebug->DEMCR |= CoreDebug_DEMCR_TRCENA_Msk;
    DWT->CYCCNT = 0;
    DWT->CTRL |= DWT_CTRL_CYCCNT_Msk;
}

uint32_t DWT_Get_CPU_Cycles(void)
{
    return DWT->CYCCNT;
}

void Measure_With_DWT(void)
{
    uint32_t cycles_start = DWT_Get_CPU_Cycles();
    
    // 执行被测代码
    for(int i = 0; i < 1000; i++) {
        ADC_GetValue();
    }
    
    uint32_t cycles_end = DWT_Get_CPU_Cycles();
    uint32_t total_cycles = cycles_end - cycles_start;
    
    printf("Total cycles: %u\n", total_cycles);
    printf("Time: %.2f ms\n", (float)total_cycles / 168000000 * 1000);
}
```

### 3.2 功耗分析

```c
// 测量不同模式下的功耗

void Power_Consumption_Test(void)
{
    // 1. 正常运行模式
    printf("Running Mode: ~50-100mA (168MHz)\n");
    
    // 2. 睡眠模式
    printf("Sleep Mode: ~10-20mA\n");
    
    // 3. 停止模式
    printf("Stop Mode: ~1-5mA\n");
    
    // 4. 待机模式
    printf("Standby Mode: <100µA\n");
}

// 优化功耗的方法
void Power_Optimization_Tips(void)
{
    // 1. 禁用不需要的外设时钟
    __HAL_RCC_GPIOB_CLK_DISABLE();
    __HAL_RCC_GPIOC_CLK_DISABLE();
    __HAL_RCC_GPIOD_CLK_DISABLE();
    __HAL_RCC_GPIOE_CLK_DISABLE();
    
    // 2. 关闭调试接口
    __HAL_DBGMCU_DISABLE();
    
    // 3. 降低主频 (需要验证功能正常)
    // 可能需要调整时序
    
    // 4. 使用睡眠模式替代忙轮询
    // 改为使用中断唤醒
    
    // 5. 优化代码算法
    // 减少循环次数，使用查表法
}
```

---

## 4. 常见调试问题

### 4.1 无法连接调试器

```
问题排查:
1. 检查接线
   ├─ SWCLK (PA14)
   ├─ SWDIO (PA13)
   └─ GND

2. 检查驱动
   └─ 安装STLINK驱动

3. 检查芯片供电
   ├─ 3.3V正常
   └─ 去耦电容

4. 尝试重置
   ├─ 按住复位键2秒
   └─ 重新连接

5. 更新固件
   └─ 更新STLINK固件
```

### 4.2 调试时程序崩溃

```
常见原因:
1. 栈溢出
   ├─ 检查栈大小
   └─ 检查递归函数

2. 看门狗触发
   ├─ 喂狗时间过长
   └─ 禁用看门狗进行调试

3. 硬件故障
   ├─ 检查复位电路
   └─ 检查晶振
```

### 4.3 打断点后不停止

```
解决方法:
1. 确保调试接口正常
2. 检查优化级别 (O0 = 无优化)
3. 尝试硬件断点
4. 检查是否进入ISR
5. 尝试复位和重新编程
```

---

## 5. 代码优化技巧

### 5.1 算法优化

```c
// 低效: 每次计算三角函数
float Get_Sine_Wave(uint8_t phase)
{
    return sinf(phase * 3.14159f / 180.0f);
}

// 高效: 使用查表
#define SINE_TABLE_SIZE  360
float sine_table[SINE_TABLE_SIZE];

void Generate_Sine_Table(void)
{
    for(int i = 0; i < 360; i++) {
        sine_table[i] = sinf(i * 3.14159f / 180.0f);
    }
}

float Get_Sine_Wave_Fast(uint8_t phase)
{
    return sine_table[phase];  // O(1) 查询
}
```

### 5.2 内存优化

```c
// 低效: 大量局部变量
void Process_Data_Inefficient(void)
{
    uint8_t buffer1[1024];
    uint8_t buffer2[1024];
    uint8_t buffer3[1024];
    // ...
}

// 高效: 复用缓冲区
uint8_t global_buffer[1024];

void Process_Data_Efficient(void)
{
    // 使用全局缓冲区
    // 减少栈使用
}
```

### 5.3 编译优化

```
Keil中的优化级别:
├─ O0: 无优化 (调试时使用)
├─ O1: 基本优化
├─ O2: 最大优化 (推荐)
├─ O3: 激进优化
└─ Oz: 最小代码尺寸

优化标志:
├─ --inline_factor=200
├─ --optimization_for_speed
├─ --no_optimize_debug
└─ --c99
```

---

## 常见问题

### Q1: 调试时运行速度慢?

✅ 禁用实时监测变量
✅ 关闭逻辑分析仪
✅ 使用条件断点而非断点
✅ 使用发布构建进行性能测试

### Q2: 代码优化后程序崩溃?

✅ 降低优化级别重试
✅ 检查浮点数精度
✅ 检查指针和数组访问
✅ 添加volatile关键字

### Q3: 如何测量真实功耗?

✅ 使用电源分析仪
✅ 在VCC路径中串接电阻测压
✅ 使用示波器测量电流
✅ 禁用所有调试功能进行测试

