# 模数转换与信号处理

## 1. ADC 模数转换器

### 1.1 ADC工作原理

```
模拟信号 (0-3.3V)
    │
    ▼
┌─────────────────┐
│  采样保持电路    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  比较器矩阵     │  (1024个比较器)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  编码器         │  (产生12位二进制码)
└────────┬────────┘
         │
         ▼
    数字值 (0-4095)

STM32F4 ADC特点:
├─ 3个独立ADC (ADC1/2/3)
├─ 12位分辨率
├─ 16个输入通道
├─ 采样率最高2.4MSPS (百万次/秒)
├─ 支持扫描模式
├─ 支持DMA
└─ 支持触发源 (软件/定时器/GPIO)
```

### 1.2 ADC精度和量化误差

```c
/*
 * 分辨率计算:
 * 
 * 量化步长 = 参考电压 / 2^分辨率
 * 对于STM32F4 (12位, 3.3V参考):
 * 量化步长 = 3.3V / 4096 = 0.805mV
 * 
 * 采样频率选择:
 * - 12位采样: 需要 12个ADC周期 + 采样周期
 * - 采样时间: 3-480个周期可配
 * - 转换时间 = 采样周期 + 12个时钟
 */

#define ADC_RESOLUTION      12          // 12位
#define VREF                3.3f        // 3.3V参考
#define ADC_MAX_VALUE       4095        // 2^12 - 1

float ADC_To_Voltage(uint16_t adc_value)
{
    return (float)adc_value * VREF / ADC_MAX_VALUE;
}

uint16_t Voltage_To_ADC(float voltage)
{
    return (uint16_t)(voltage / VREF * ADC_MAX_VALUE);
}
```

### 1.3 ADC初始化

```c
#include "stm32f4xx_hal.h"

ADC_HandleTypeDef hadc1;

void ADC1_Init(void)
{
    // 启用时钟
    __HAL_RCC_ADC1_CLK_ENABLE();
    __HAL_RCC_GPIOA_CLK_ENABLE();
    
    // 配置GPIO PA0 (ADC_IN0)
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    GPIO_InitStruct.Pin = GPIO_PIN_0;
    GPIO_InitStruct.Mode = GPIO_MODE_ANALOG;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
    
    // 配置ADC
    hadc1.Instance = ADC1;
    hadc1.Init.ClockPrescaler = ADC_CLOCK_SYNC_PCLK_DIV2;     // 时钟分频
    hadc1.Init.Resolution = ADC_RESOLUTION_12B;
    hadc1.Init.ScanConvMode = DISABLE;
    hadc1.Init.ContinuousConvMode = DISABLE;
    hadc1.Init.DiscontinuousConvMode = DISABLE;
    hadc1.Init.ExternalTrigConvEdge = ADC_EXTERNALTRIGCONVEDGE_NONE;
    hadc1.Init.ExternalTrigConv = ADC_SOFTWARE_START;
    hadc1.Init.DataAlign = ADC_DATAALIGN_RIGHT;
    hadc1.Init.NbrOfConversion = 1;
    hadc1.Init.DMAContinuousRequests = DISABLE;
    hadc1.Init.EOCSelection = ADC_EOC_SINGLE_CONV;
    
    HAL_ADC_Init(&hadc1);
    
    // 配置通道
    ADC_ChannelConfTypeDef sConfig = {0};
    sConfig.Channel = ADC_CHANNEL_0;
    sConfig.Rank = 1;
    sConfig.SamplingTime = ADC_SAMPLETIME_84CYCLES;    // 采样时间
    
    HAL_ADC_ConfigChannel(&hadc1, &sConfig);
}

// 单次采样
uint16_t ADC_GetValue(void)
{
    HAL_ADC_Start(&hadc1);
    HAL_ADC_PollForConversion(&hadc1, 100);
    uint16_t value = HAL_ADC_GetValue(&hadc1);
    HAL_ADC_Stop(&hadc1);
    return value;
}

// 连续采样 (不阻塞)
void ADC_Start_Continuous(void)
{
    HAL_ADC_Start(&hadc1);
}

uint16_t ADC_Get_Current_Value(void)
{
    return HAL_ADC_GetValue(&hadc1);
}
```

### 1.4 多通道采样

```c
#define CHANNEL_COUNT   3
uint16_t adc_values[CHANNEL_COUNT];

void ADC1_MultiChannel_Init(void)
{
    // 配置3个通道: PA0, PA1, PA2
    
    ADC_ChannelConfTypeDef sConfig = {0};
    
    // 通道1: PA0
    sConfig.Channel = ADC_CHANNEL_0;
    sConfig.Rank = 1;
    sConfig.SamplingTime = ADC_SAMPLETIME_84CYCLES;
    HAL_ADC_ConfigChannel(&hadc1, &sConfig);
    
    // 通道2: PA1
    sConfig.Channel = ADC_CHANNEL_1;
    sConfig.Rank = 2;
    HAL_ADC_ConfigChannel(&hadc1, &sConfig);
    
    // 通道3: PA2
    sConfig.Channel = ADC_CHANNEL_2;
    sConfig.Rank = 3;
    HAL_ADC_ConfigChannel(&hadc1, &sConfig);
    
    // 修改ADC配置
    hadc1.Init.NbrOfConversion = 3;
    hadc1.Init.ScanConvMode = ENABLE;
    HAL_ADC_Init(&hadc1);
}

void ADC_Scan_Conversion(void)
{
    HAL_ADC_Start(&hadc1);
    for(int i = 0; i < CHANNEL_COUNT; i++) {
        HAL_ADC_PollForConversion(&hadc1, 100);
        adc_values[i] = HAL_ADC_GetValue(&hadc1);
    }
    HAL_ADC_Stop(&hadc1);
}
```

---

## 2. DAC 数模转换器

### 2.1 DAC工作原理

```
数字值 (0-4095)
    │
    ▼
┌─────────────────┐
│  DAC转换器      │  (12位R-2R梯形网络)
└────────┬────────┘
         │
         ▼
    模拟电压 (0-3.3V)

STM32F4 DAC特点:
├─ 2个独立DAC (DAC1/2)
├─ 12位分辨率
├─ 支持DMA
├─ 支持波形生成
├─ 支持触发源
└─ 转换时间 < 1µs
```

### 2.2 DAC初始化

```c
DAC_HandleTypeDef hdac;

void DAC1_Init(void)
{
    // 启用时钟
    __HAL_RCC_DAC_CLK_ENABLE();
    __HAL_RCC_GPIOA_CLK_ENABLE();
    
    // 配置GPIO PA4 (DAC_OUT1)
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    GPIO_InitStruct.Pin = GPIO_PIN_4;
    GPIO_InitStruct.Mode = GPIO_MODE_ANALOG;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
    
    // 配置DAC
    hdac.Instance = DAC;
    HAL_DAC_Init(&hdac);
    
    // 配置DAC通道1
    DAC_ChannelConfTypeDef sConfig = {0};
    sConfig.DAC_Trigger = DAC_TRIGGER_SOFTWARE;       // 软件触发
    sConfig.DAC_OutputBuffer = DAC_OUTPUTBUFFER_ENABLE;
    
    HAL_DAC_ConfigChannel(&hdac, &sConfig, DAC_CHANNEL_1);
}

// 输出固定电压
void DAC_SetVoltage(float voltage)
{
    uint16_t dac_value = (uint16_t)(voltage / 3.3f * 4095);
    HAL_DAC_SetValue(&hdac, DAC_CHANNEL_1, DAC_ALIGN_12B_R, dac_value);
    HAL_DAC_Start(&hdac, DAC_CHANNEL_1);
}

// 输出三角波
void DAC_Triangle_Wave(void)
{
    for(uint16_t i = 0; i < 4096; i += 10) {
        HAL_DAC_SetValue(&hdac, DAC_CHANNEL_1, DAC_ALIGN_12B_R, i);
        HAL_Delay(1);
    }
    for(uint16_t i = 4095; i > 0; i -= 10) {
        HAL_DAC_SetValue(&hdac, DAC_CHANNEL_1, DAC_ALIGN_12B_R, i);
        HAL_Delay(1);
    }
}

// 输出正弦波
#include <math.h>
#define SINE_SAMPLES  100
uint16_t sine_table[SINE_SAMPLES];

void Generate_Sine_Table(void)
{
    for(int i = 0; i < SINE_SAMPLES; i++) {
        float sine = sinf(2 * 3.14159f * i / SINE_SAMPLES);
        sine_table[i] = (uint16_t)((sine + 1) / 2 * 4095);
    }
}

void DAC_Output_Sine(void)
{
    static int index = 0;
    HAL_DAC_SetValue(&hdac, DAC_CHANNEL_1, DAC_ALIGN_12B_R, sine_table[index]);
    index = (index + 1) % SINE_SAMPLES;
}
```

---

## 3. 信号采集和处理

### 3.1 滤波器设计

```c
// 一阶低通滤波器 (RC滤波)
typedef struct {
    float alpha;        // 滤波系数 (0-1)
    float last_value;   // 上一次输出
} LPFilter_t;

void LPFilter_Init(LPFilter_t *filter, float alpha)
{
    filter->alpha = alpha;
    filter->last_value = 0;
}

float LPFilter_Update(LPFilter_t *filter, float new_value)
{
    filter->last_value = filter->alpha * new_value + 
                        (1 - filter->alpha) * filter->last_value;
    return filter->last_value;
}

// 使用例: alpha = 0.1 (灵敏度更低，滤波更强)
LPFilter_t filter;
LPFilter_Init(&filter, 0.1f);

float filtered_value = LPFilter_Update(&filter, raw_adc_value * 3.3f / 4095);
```

### 3.2 均值滤波

```c
#define FILTER_SIZE  10

typedef struct {
    uint16_t buffer[FILTER_SIZE];
    uint8_t index;
} MeanFilter_t;

void MeanFilter_Init(MeanFilter_t *filter)
{
    filter->index = 0;
    for(int i = 0; i < FILTER_SIZE; i++) {
        filter->buffer[i] = 0;
    }
}

uint16_t MeanFilter_Update(MeanFilter_t *filter, uint16_t new_value)
{
    filter->buffer[filter->index] = new_value;
    filter->index = (filter->index + 1) % FILTER_SIZE;
    
    uint32_t sum = 0;
    for(int i = 0; i < FILTER_SIZE; i++) {
        sum += filter->buffer[i];
    }
    return sum / FILTER_SIZE;
}
```

### 3.3 中值滤波

```c
#define MEDIAN_SIZE  5

uint16_t Median_Filter(uint16_t *values, int count)
{
    // 冒泡排序
    for(int i = 0; i < count - 1; i++) {
        for(int j = 0; j < count - i - 1; j++) {
            if(values[j] > values[j + 1]) {
                uint16_t temp = values[j];
                values[j] = values[j + 1];
                values[j + 1] = temp;
            }
        }
    }
    // 返回中值
    return values[count / 2];
}

// 使用
uint16_t median_values[MEDIAN_SIZE];
uint16_t filtered = Median_Filter(median_values, MEDIAN_SIZE);
```

---

## 4. 传感器接口示例

### 4.1 温度传感器 (NTC热敏电阻)

```c
// NTC热敏电阻 - 温度系数负值
// R(T) = R0 * exp(β * (1/T - 1/T0))

#define R0          10000       // 25℃下的阻值 (10K)
#define BETA        3977        // β系数
#define T0          (25 + 273.15) // 25℃ = 298.15K

float NTC_GetTemperature(uint16_t adc_value)
{
    // ADC值转换为电压
    float voltage = adc_value * 3.3f / 4095;
    
    // 分压电路: V_NTC = Vcc * R_NTC / (R_series + R_NTC)
    // 假设 R_series = 10K
    #define R_SERIES    10000
    
    float R_ntc = R_SERIES * voltage / (3.3f - voltage);
    
    // 利用Steinhart-Hart方程
    float ln_r = logf(R_ntc / R0);
    float T = 1.0f / (1.0f/T0 + ln_r / BETA);
    
    return T - 273.15;  // 转换为摄氏度
}
```

### 4.2 光敏电阻 (LDR)

```c
// LDR光敏电阻 - 亮度越高，阻值越低

#define LDR_R_BRIGHT    1000    // 亮度环境阻值 (1K)
#define LDR_R_DARK      100000  // 暗环境阻值 (100K)

float LDR_GetIlluminance(uint16_t adc_value)
{
    float voltage = adc_value * 3.3f / 4095;
    
    // 分压电路
    #define R_SERIES    10000
    float R_ldr = R_SERIES * voltage / (3.3f - voltage);
    
    // 粗略的亮度值 (0-100)
    float illuminance = 100 * (LDR_R_DARK - R_ldr) / (LDR_R_DARK - LDR_R_BRIGHT);
    
    return illuminance;
}
```

---

## 常见问题

### Q1: ADC采样值不稳定?

✅ 增加采样时间
✅ 使用滤波器 (低通/均值/中值)
✅ 检查模拟电源去耦电容
✅ 减少模拟信号线干扰

### Q2: DAC输出不稳定?

✅ 增加输出缓冲容量
✅ 检查参考电压稳定性
✅ 减少负载电流
✅ 增加输出RC滤波

### Q3: 如何实现高精度测量?

✅ 使用多次采样取均值
✅ 采用过采样技术 (增加有效位)
✅ 校准零点和满度
✅ 使用更好的参考源

