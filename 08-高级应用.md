# 高级应用与系统设计

## 1. 自启动程序 (Bootloader)

### 1.1 Bootloader原理

```
启动流程:
┌─────────────────────────────────┐
│  Power On / Reset               │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│ 启动文件 (Startup)               │
│ 加载栈指针和中断向量表          │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│ Bootloader (可选)                │
│ ├─ 检查固件有效性               │
│ ├─ 处理固件升级                 │
│ └─ 跳转到应用程序               │
└──��───────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│ 应用程序 (Application)          │
│ 主程序执行                      │
└─────────────────────────────────┘

Flash分布:
┌──────────────────────┐ 0x080FFFFF
│  应用程序区          │
│  0x08010000-0x080FFFFF
├──────────────────────┤ 0x08010000
│  Bootloader区        │
│  0x08000000-0x0800FFFF
└──────────────────────┘ 0x08000000
```

### 1.2 Bootloader实现

```c
// Bootloader代码 (位于0x08000000)

typedef void (*pFunction)(void);

#define APP_START_ADDR      0x08010000  // 应用程序起始地址
#define APP_SIZE            0x0F0000    // 应用程序大小

void Jump_To_Application(void)
{
    // 获取应用程序的栈顶指针和复位向量
    uint32_t *app_vector = (uint32_t *)APP_START_ADDR;
    uint32_t app_stack = app_vector[0];
    uint32_t app_entry = app_vector[1];
    
    // 检��有效性
    if((app_stack & 0x2FFF0000) != 0x20000000) {
        // 栈指针无效
        while(1);
    }
    
    // 关闭中断
    __disable_irq();
    
    // 设置栈指针
    __set_MSP(app_stack);
    
    // 禁用所有中断并清除所有待处理中断
    for(int i = 0; i < 8; i++) {
        NVIC->ICER[i] = 0xFFFFFFFF;
        NVIC->ICPR[i] = 0xFFFFFFFF;
    }
    
    // 重新映射NVIC向量表到应用程序区
    NVIC_SetVectorTable(NVIC_VectorTable_FLASH, APP_START_ADDR - 0x08000000);
    
    // 跳转到应用程序
    pFunction Jump = (pFunction)app_entry;
    Jump();
}

void Bootloader_Main(void)
{
    SystemClock_Config();
    
    // 检查升级标志
    if(Check_Update_Flag()) {
        // 执行固件升级
        Firmware_Update();
    }
    
    // 跳转到应用程序
    Jump_To_Application();
    
    // 不会执行到这里
    while(1);
}
```

---

## 2. 固件升级 (OTA/IAP)

### 2.1 固件升级机制

```
在应用程序中实现固件升级:

1. 接收新固件 (UART/网络)
   ├─ 通过UART、SD卡或网络接收固件
   └─ 存储到Flash的升级区域

2. 验证固件
   ├─ 校验CRC/MD5
   ├─ 检查签名
   └─ 验证有效性

3. 标记升级标志
   ├─ 在SRAM或Flash中设置标志
   └─ 触发软复位

4. Bootloader处理
   ├─ 检查升级标志
   ├─ 复制新固件到应用区
   └─ 清除升级标志

5. 启动新固件
   └─ 跳转到新应用程序
```

### 2.2 固件升级实现

```c
#define UPDATE_FLAG_ADDR    0x0800F000  // SRAM用于标志
#define UPDATE_FLAG_VALUE   0xDEADBEEF

#define FIRMWARE_AREA       0x08020000  // 新固件存储区
#define FIRMWARE_MAX_SIZE   0x0E0000

typedef struct {
    uint32_t size;                      // 固件大小
    uint32_t crc32;                     // CRC校验值
    uint8_t version[4];                 // 版本号
} FirmwareHeader_t;

// 接收新固件 (通过UART)
void Receive_Firmware_UART(void)
{
    FirmwareHeader_t header;
    
    // 接收头部
    HAL_UART_Receive(&huart1, (uint8_t *)&header, sizeof(header), 5000);
    
    if(header.size > FIRMWARE_MAX_SIZE) {
        // 固件过大
        return;
    }
    
    // 擦除Flash
    Flash_Erase(FIRMWARE_AREA, header.size);
    
    // 接收固件数据
    uint32_t received = 0;
    uint8_t buffer[256];
    
    while(received < header.size) {
        uint32_t to_receive = (header.size - received > 256) ? 256 : (header.size - received);
        HAL_UART_Receive(&huart1, buffer, to_receive, 5000);
        
        // 写入Flash
        Flash_Write(FIRMWARE_AREA + received, buffer, to_receive);
        
        received += to_receive;
    }
    
    // 校验
    uint32_t calc_crc = CRC32_Calculate((uint8_t *)FIRMWARE_AREA, header.size);
    
    if(calc_crc != header.crc32) {
        // CRC校验失败
        return;
    }
    
    // 设置升级标志
    *(uint32_t *)UPDATE_FLAG_ADDR = UPDATE_FLAG_VALUE;
    
    // 软复位
    HAL_NVIC_SystemReset();
}

// Bootloader中执行升级
void Bootloader_Execute_Update(void)
{
    if(*(uint32_t *)UPDATE_FLAG_ADDR != UPDATE_FLAG_VALUE) {
        return;  // 没有升级标志
    }
    
    FirmwareHeader_t *header = (FirmwareHeader_t *)FIRMWARE_AREA;
    
    // 擦除应用区
    Flash_Erase(APP_START_ADDR, header->size);
    
    // 复制新固件到应用区
    for(uint32_t i = 0; i < header->size; i += 256) {
        uint32_t copy_size = (header->size - i > 256) ? 256 : (header->size - i);
        Flash_Write(APP_START_ADDR + i, 
                   (uint8_t *)FIRMWARE_AREA + i, 
                   copy_size);
    }
    
    // 清除升级标志
    *(uint32_t *)UPDATE_FLAG_ADDR = 0;
}
```

---

## 3. RTOS集成

### 3.1 FreeRTOS基本任务

```c
#include "FreeRTOS.h"
#include "task.h"

// 任务优先级
#define TASK1_PRIORITY      2
#define TASK2_PRIORITY      1

// 任务栈大小
#define TASK_STACK_SIZE     256

// 任务函数
void Task1_Handler(void *pvParameters)
{
    while(1) {
        // 任务代码
        HAL_GPIO_TogglePin(GPIOD, GPIO_PIN_12);  // LED
        
        // 延时 100ms
        vTaskDelay(pdMS_TO_TICKS(100));
    }
}

void Task2_Handler(void *pvParameters)
{
    while(1) {
        // 任务代码
        uint16_t adc_val = ADC_GetValue();
        
        // 延时 50ms
        vTaskDelay(pdMS_TO_TICKS(50));
    }
}

// 创建任务
void Create_Tasks(void)
{
    xTaskCreate(Task1_Handler, 
               "Task1", 
               TASK_STACK_SIZE, 
               NULL, 
               TASK1_PRIORITY, 
               NULL);
    
    xTaskCreate(Task2_Handler,
               "Task2",
               TASK_STACK_SIZE,
               NULL,
               TASK2_PRIORITY,
               NULL);
    
    // 启动调度器
    vTaskStartScheduler();
}
```

### 3.2 任务间通信 (消息队列)

```c
#include "queue.h"

QueueHandle_t xQueue = NULL;

#define QUEUE_LENGTH    10
#define QUEUE_ITEM_SIZE sizeof(uint32_t)

void Create_Queue(void)
{
    xQueue = xQueueCreate(QUEUE_LENGTH, QUEUE_ITEM_SIZE);
}

void Producer_Task(void *pvParameters)
{
    uint32_t data = 0;
    
    while(1) {
        data++;
        
        // 发送数据到队列
        xQueueSend(xQueue, &data, portMAX_DELAY);
        
        vTaskDelay(pdMS_TO_TICKS(100));
    }
}

void Consumer_Task(void *pvParameters)
{
    uint32_t received_data;
    
    while(1) {
        // 从队列接收数据
        if(xQueueReceive(xQueue, &received_data, pdMS_TO_TICKS(1000))) {
            // 处理接收到的数据
        }
    }
}
```

---

## 4. 调试与优化

### 4.1 JTAG/SWD调试

```
调试接口:
├─ JTAG (5针)
│  ├─ TCK (时钟)
│  ├─ TMS (模式选择)
│  ├─ TDI (数据输入)
│  ├─ TDO (数据输出)
│  └─ GND
│
└─ SWD (2针) ⭐ (推荐)
   ├─ SWCLK (时钟)
   ├─ SWDIO (数据)
   └─ GND

Keil调试技巧:
├─ 实时变量监测
├─ 内存查看器
├─ 寄存器观察
├─ 逻辑分析仪
└─ 性能分析
```

### 4.2 性能优化

```c
// 代码执行时间测量
void Measure_Execution_Time(void)
{
    uint32_t start_tick = HAL_GetTick();
    
    // 被测代码
    for(int i = 0; i < 1000; i++) {
        ADC_GetValue();
    }
    
    uint32_t end_tick = HAL_GetTick();
    uint32_t execution_time = end_tick - start_tick;  // ms
}

// 使用SystemCoreClockUpdate()更新系统时钟
void Update_System_Clock(void)
{
    SystemCoreClockUpdate();
    
    // SystemCoreClock 包含当前系统时钟频率
}

// 内存使用分析
void Analyze_Memory_Usage(void)
{
    // 获取SRAM使用情况
    extern uint32_t _sbss, _ebss;
    extern uint32_t _sdata, _edata;
    
    uint32_t bss_size = &_ebss - &_sbss;
    uint32_t data_size = &_edata - &_sdata;
    
    // bss_size + data_size = 全局数据占用
}
```

### 4.3 功耗测试

```c
// 不同模式下的功耗测试

void Power_Test_Run_Mode(void)
{
    // 正常运行模式: ~50-100mA (168MHz)
    HAL_Delay(1000);
}

void Power_Test_Sleep_Mode(void)
{
    // 睡眠模式: ~10-20mA
    while(1) {
        HAL_PWR_EnterSLEEPMode(PWR_MAINREGULATOR_ON, PWR_SLEEPENTRY_WFI);
    }
}

void Power_Test_Stop_Mode(void)
{
    // 停止模式: ~1-5mA
    while(1) {
        HAL_PWR_EnterSTOPMode(PWR_MAINREGULATOR_ON, PWR_STOPENTRY_WFI);
    }
}

void Power_Test_Standby_Mode(void)
{
    // 待机模式: <100µA
    HAL_PWR_EnterSTANDBYMode();
}
```

---

## 常见问题

### Q1: 如何安全地升级固件?

✅ 使用双固件备份
✅ 校验固件完整性 (CRC/SHA)
✅ 升级失败时回滚
✅ 记录升级历史

### Q2: RTOS会不会很耗资源?

✅ FreeRTOS占用很少资源 (~1-2KB)
✅ 多任务管理的收益远大于开销
✅ 适合复杂应用

### Q3: 如何获取精确的执行时间?

✅ 使用DWT (Data Watchpoint and Trace)
✅ 使用硬件定时器计数
✅ 避免在中断中测量

