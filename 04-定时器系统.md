# 定时器系统

## 1. STM32定时器分类

### 1.1 定时器类型

```
定时器系统
│
├─ 高级定时器 (Advanced Timer)
│  ├─ TIM1 (APB2, 168MHz)
│  ├─ TIM8 (APB2, 168MHz)
│  └─ 功能: PWM, 死区时间, 刹车保护
│
├─ 通用定时器 (General Purpose)
│  ├─ TIM2/3/4/5 (APB1, 84*2=168MHz)
│  └─ 功能: PWM, 输入捕获, 编码器
│
├─ 基本定时器 (Basic Timer)
│  ├─ TIM6/7 (APB1, 84*2=168MHz)
│  └─ 功能: 定时中断, DAC触发
│
└─ 看门狗 (Watchdog)
   ├─ IWDG (独立看门狗)
   └─ WWDG (窗口看门狗)
```

### 1.2 定时器时钟

```c
// 重要规则: APB预分频 > 1时，定时器时钟自动 ×2

// STM32F4默认配置
// SYSCLK = 168MHz
// AHB = 168MHz (分频系数 1)
// APB1 = 84MHz (分频系数 2)  -> TIM2/3/4/5时钟 = 168MHz
// APB2 = 168MHz (分频系数 1) -> TIM1/8时钟 = 168MHz

#define APB1_CLK        84000000      // 84MHz
#define TIM2_CLK        (2 * APB1_CLK) // 168MHz
```

---

## 2. 基本定时中断

### 2.1 定时器初始化

```c
#include "stm32f4xx_hal.h"

TIM_HandleTypeDef tim2_handle;

void TIM2_Init(void)
{
    // 启用时钟
    __HAL_RCC_TIM2_CLK_ENABLE();
    
    // 配置定时器
    // 期望: 1ms 产生一次中断
    // TIM2时钟 = 168MHz
    
    tim2_handle.Instance = TIM2;
    tim2_handle.Init.Prescaler = 1680 - 1;              // 分频: 168MHz/1680 = 100KHz
    tim2_handle.Init.CounterMode = TIM_COUNTERMODE_UP;  // 向上计数
    tim2_handle.Init.Period = 100 - 1;                  // 重装: 100/100KHz = 1ms
    tim2_handle.Init.ClockDivision = TIM_CLOCKDIVISION_DIV1;
    
    HAL_TIM_Base_Init(&tim2_handle);
    
    // 配置NVIC
    HAL_NVIC_SetPriority(TIM2_IRQn, 1, 0);
    HAL_NVIC_EnableIRQ(TIM2_IRQn);
    
    // 启动中断
    HAL_TIM_Base_Start_IT(&tim2_handle);
}

// 中断服务程序
void TIM2_IRQHandler(void)
{
    HAL_TIM_IRQHandler(&tim2_handle);
}

// 中断回调
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if(htim->Instance == TIM2) {
        // 每1ms执行一次
        LED_Toggle();
    }
}
```

### 2.2 周期计算

```c
/*
 * 定时器周期计算公式:
 * 
 * 计数频率 = 定时器时钟 / (PSC + 1)
 * 周期 = (ARR + 1) / 计数频率
 * 
 * 例: 需要 10ms 中断
 * TIM_CLK = 168MHz
 * 设置PSC = 1680-1, 计数频率 = 100KHz
 * 需要ARR = 1000-1 (使计数1000次)
 * 周期 = 1000 / 100KHz = 10ms
 */

#define TIM_CLK              168000000   // 168MHz
#define DESIRE_COUNT_FREQ    100000      // 100KHz计数频率

void Calculate_Timer_Params(uint32_t ms, 
                            uint32_t *psc, 
                            uint32_t *arr)
{
    // 计算预分频器
    *psc = TIM_CLK / DESIRE_COUNT_FREQ - 1;
    
    // 计算自动重装值
    *arr = DESIRE_COUNT_FREQ / 1000 * ms - 1;
}

// 例子: 5ms定时
uint32_t psc, arr;
Calculate_Timer_Params(5, &psc, &arr);
// psc = 1680-1 = 1679
// arr = 100000/1000*5 - 1 = 499
```

---

## 3. PWM输出

### 3.1 PWM基本原理

```
PWM (Pulse Width Modulation) 脉宽调制

设置 ARR=1000, CCR=300 (占空比30%)

计数器值  ▲
         │   ┌────────────────────────────────┐
    1000 │   │ CCR=300                        │
         │   │ ├┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┬┤
         │   ├─┘└─────────────────────────────┘─ PWM输出
         │  /│
       300 │  │  ┌─ 高电平 (300个周期)
         │ / │  │
         │/  │  └─ 低电平 (700个周期)
      0  └───┴──────────────────────────────────> 时间
         0   1000 2000 3000 ...

占空比 = CCR / (ARR+1) * 100% = 300/1000 * 100% = 30%
```

### 3.2 PWM初始化

```c
TIM_HandleTypeDef tim3_handle;

void TIM3_PWM_Init(void)
{
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    TIM_OC_InitTypeDef sConfigOC = {0};
    
    // 启用时钟
    __HAL_RCC_TIM3_CLK_ENABLE();
    __HAL_RCC_GPIOA_CLK_ENABLE();
    
    // 配置GPIO PA6 为 TIM3_CH1
    GPIO_InitStruct.Pin = GPIO_PIN_6;
    GPIO_InitStruct.Mode = GPIO_MODE_AF_PP;        // 复用推挽
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    GPIO_InitStruct.Alternate = GPIO_AF2_TIM3;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
    
    // 配置定时器
    // 期望: 20KHz PWM, 占空比0-100%可调
    // PSC = 8-1, 计数频率 = 168MHz / 8 = 21MHz
    // ARR = 1050-1, PWM频率 = 21MHz / 1050 = 20KHz
    
    tim3_handle.Instance = TIM3;
    tim3_handle.Init.Prescaler = 8 - 1;
    tim3_handle.Init.CounterMode = TIM_COUNTERMODE_UP;
    tim3_handle.Init.Period = 1050 - 1;
    tim3_handle.Init.ClockDivision = TIM_CLOCKDIVISION_DIV1;
    
    HAL_TIM_PWM_Init(&tim3_handle);
    
    // 配置输出比较 (PWM)
    sConfigOC.OCMode = TIM_OCMODE_PWM1;
    sConfigOC.Pulse = 525;                      // 初始占空比 50%
    sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;
    sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;
    
    HAL_TIM_PWM_ConfigChannel(&tim3_handle, &sConfigOC, TIM_CHANNEL_1);
    
    // 启动PWM
    HAL_TIM_PWM_Start(&tim3_handle, TIM_CHANNEL_1);
}

// 改变PWM占空比
void PWM_SetDuty(uint32_t duty_percent)
{
    // duty_percent: 0-100
    uint32_t pulse = (tim3_handle.Init.Period + 1) * duty_percent / 100;
    __HAL_TIM_SET_COMPARE(&tim3_handle, TIM_CHANNEL_1, pulse);
}

// 使用示例
PWM_SetDuty(0);    // 0% 占空比
PWM_SetDuty(50);   // 50% 占空比
PWM_SetDuty(100);  // 100% 占空比
```

### 3.3 PWM计算

```c
/*
 * PWM参数计算:
 * 
 * PWM频率 (Hz) = 定时器时钟 / (PSC+1) / (ARR+1)
 * 占空比 (%) = CCR / (ARR+1) * 100
 * 
 * 常见应用:
 * - LED调光: 100Hz-1KHz
 * - 电机控制: 10KHz-20KHz
 * - 舵机: 50Hz
 */

#define PWM_FREQ_20KHZ  20000
#define PWM_FREQ_50HZ   50

void Calculate_PWM_Params(uint32_t freq,
                          uint32_t tim_clk,
                          uint32_t *psc,
                          uint32_t *arr)
{
    // 选择预分频器使计数频率在合理范围
    *psc = 8 - 1;
    uint32_t count_freq = tim_clk / (*psc + 1);
    
    // 计算自动重装值
    *arr = count_freq / freq - 1;
}

// 实际应用: 10个LED调光 (分别接TIM3的4个通道)
void LED_PWM_Init(void)
{
    // TIM3支持4个通道 CH1-CH4
    // 分别输出不同占空比的PWM
}
```

---

## 4. 输入捕获

### 4.1 频率测量

```c
// 使用输入捕获测量外部信号频率

TIM_HandleTypeDef tim4_handle;

void TIM4_InputCapture_Init(void)
{
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    TIM_IC_InitTypeDef sConfigIC = {0};
    
    __HAL_RCC_TIM4_CLK_ENABLE();
    __HAL_RCC_GPIOD_CLK_ENABLE();
    
    // 配置 PD12 为 TIM4_CH1
    GPIO_InitStruct.Pin = GPIO_PIN_12;
    GPIO_InitStruct.Mode = GPIO_MODE_AF_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    GPIO_InitStruct.Alternate = GPIO_AF2_TIM4;
    HAL_GPIO_Init(GPIOD, &GPIO_InitStruct);
    
    // 配置定时器
    // 预分频 = 168-1, 计数频率 = 1MHz (1µs精度)
    tim4_handle.Instance = TIM4;
    tim4_handle.Init.Prescaler = 168 - 1;
    tim4_handle.Init.CounterMode = TIM_COUNTERMODE_UP;
    tim4_handle.Init.Period = 0xFFFF;              // 16位最大值
    
    HAL_TIM_IC_Init(&tim4_handle);
    
    // 配置输入捕获 (上升沿触发)
    sConfigIC.ICPolarity = TIM_ICPOLARITY_RISING;
    sConfigIC.ICSelection = TIM_ICSELECTION_DIRECTTI;
    sConfigIC.ICPrescaler = TIM_ICPSC_DIV1;
    sConfigIC.ICFilter = 0;
    
    HAL_TIM_IC_ConfigChannel(&tim4_handle, &sConfigIC, TIM_CHANNEL_1);
    
    // 配置NVIC
    HAL_NVIC_SetPriority(TIM4_IRQn, 1, 0);
    HAL_NVIC_EnableIRQ(TIM4_IRQn);
    
    // 启动捕获
    HAL_TIM_IC_Start_IT(&tim4_handle, TIM_CHANNEL_1);
}

// 处理捕获结果
uint32_t last_capture = 0;
uint32_t measured_freq = 0;

void HAL_TIM_IC_CaptureCallback(TIM_HandleTypeDef *htim)
{
    if(htim->Instance == TIM4) {
        uint32_t capture = HAL_TIM_ReadCapturedValue(htim, TIM_CHANNEL_1);
        
        if(last_capture == 0) {
            last_capture = capture;
        } else {
            uint32_t period = capture - last_capture;
            // 频率 = 1MHz / period (Hz)
            measured_freq = 1000000 / period;
            last_capture = capture;
        }
    }
}

void TIM4_IRQHandler(void)
{
    HAL_TIM_IRQHandler(&tim4_handle);
}
```

### 4.2 脉宽测量

```c
// 同时使用CH1和CH2测量脉宽
// CH1: 上升沿捕获 (脉冲开始)
// CH2: 下降沿捕获 (脉冲结束)

void TIM5_Pulse_Width_Init(void)
{
    // 配置TIM5 CH1 (上升沿) 和 CH2 (下降沿)
    // 计算脉宽 = CCR2 - CCR1
}
```

---

## 5. 定时器常见问题

### Q1: PWM为什么没有输出?

✅ 检查GPIO时钟和定时器时钟
✅ 检查GPIO复用功能配置
✅ 检查CCR寄存器是否大于0
✅ 使用示波器测量PA6是否有信号

### Q2: 定时中断触发频率不对?

✅ 验证PSC和ARR计算
✅ 检查定时器时钟 (×2规则)
✅ 检查中断标志位清除
✅ 使用逻辑分析仪测量实际频率

### Q3: 定时器与其他外设冲突?

✅ 检查引脚是否被多个外设占用
✅ 查看数据手册中的复用功能表
✅ 使用不同的定时器和GPIO

---

## 6. 实战应用案例

### 案例1: LED呼吸灯

```c
// 利用PWM产生呼吸灯效果

void LED_Breathing_Task(void)
{
    static uint32_t duty = 0;
    static int8_t direction = 1;  // 1: 增加, -1: 减少
    
    duty += direction;
    
    if(duty >= 100) {
        duty = 100;
        direction = -1;
    } else if(duty <= 0) {
        duty = 0;
        direction = 1;
    }
    
    PWM_SetDuty(duty);
    
    // 在SysTick或定时器中断中每10ms调用一次
    // 呼吸周期 = 100 * 10ms * 2 = 2秒
}
```

### 案例2: 电机速度控制

```c
// 通过PWM控制直流电机速度

void Motor_SetSpeed(uint32_t speed_percent)
{
    // speed_percent: 0-100
    PWM_SetDuty(speed_percent);
}

// 加速/减速控制
void Motor_AccelControl(void)
{
    static uint32_t current_speed = 0;
    uint32_t target_speed = 80;
    
    if(current_speed < target_speed) {
        current_speed += 2;  // 2% 每次
    }
    
    Motor_SetSpeed(current_speed);
}
```

